when:
- event: [push, tag]

steps:
- name: Test
  image: ghcr.io/astral-sh/uv:debian-slim
  commands:
    - uv sync --frozen --no-install-project
    - uv run ruff check .
    - uv run ruff format --check .
    - uv run ty check .